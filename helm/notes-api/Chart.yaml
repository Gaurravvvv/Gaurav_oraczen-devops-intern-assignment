apiVersion: v2
name: notes-api
description: A production-ready Helm chart for FastAPI Notes API with PostgreSQL subchart dependency
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: postgresql
    version: "15.5.37"
    repository: "oci://registry-1.docker.io/bitnamicharts"
    condition: postgresql.enabled
